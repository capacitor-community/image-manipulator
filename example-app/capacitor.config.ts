import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ryltsov.alex.image.manipulator.demo',
  appName: 'example-app',
  webDir: 'www/browser'
};

export default config;
